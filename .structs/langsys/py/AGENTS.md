# Python Alias

Use `../python/AGENTS.md` as the canonical Python language standard.

The `py/` directory is retained as a legacy alias. New standards and updates
should be made under `structs/langsys/python/`.
